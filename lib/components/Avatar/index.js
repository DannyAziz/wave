import React from 'react';

import * as Styled from './styled';

const Avatar = props => (
  <Styled.Avatar>
    <Styled.AvatarImage />
    <Styled.AvatarText>Hello World</Styled.AvatarText>
  </Styled.Avatar>
);

export default Avatar;
